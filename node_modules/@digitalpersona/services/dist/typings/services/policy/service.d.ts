import { User } from '@digitalpersona/core';
import { ResourceActions } from '@digitalpersona/services/dist/typings/commonona/services/dist/typings/com@digitalpersona/services/dist/typings/private { Service } from '@digitalperso@digitalpersona/services/dist/typings/services/policy/policyInfodist/typings/private';
import { Pol@digitalpersona/services/dist/typings/services/policy/stepUp} from '@digitalpersona/services/dist/typings/services/policy/policyInfo';
import { ContextualInfo } from '@digitalpersona/services/dist/typings/services/policy/stepUp';
/**
 * DigitalPersona Web Policy (DPWebPolicy) service interface.
 */
export interface IPolicyService {
    GetPolicyInfo(user: User, resourceUri: string, action: ResourceActions, info: ContextualInfo): Promise<PolicyInfo>;
}
/**
 * DigitalPersona Web Policy (DPWebPolicy) service client wrapper.
 */
export declare class PolicyService extends Service implements IPolicyService {
    /** Constructs a service wrapper.
     * @param endpointUrl - a URL to the DPWebClaims service.
     */
    constructor(endpointUrl: string);
    /** @inheritdoc */
    GetPolicyInfo(user: User, resourceUri: string, action: ResourceActions, info: ContextualInfo): Promise<PolicyInfo>;
}
